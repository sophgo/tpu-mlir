set(BACKEND_CUSTOM_OUTPUT_FILE backend_custom)
add_library(${BACKEND_CUSTOM_OUTPUT_FILE} SHARED ${SRC_FILES})
set_target_properties(${BACKEND_CUSTOM_OUTPUT_FILE} PROPERTIES LINK_FLAGS "-g")
install(TARGETS ${BACKEND_CUSTOM_OUTPUT_FILE} LIBRARY DESTINATION lib)
