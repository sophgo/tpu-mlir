set(PLUGIN_CUSTOM_OUTPUT_FILE plugin_custom)
add_library(${PLUGIN_CUSTOM_OUTPUT_FILE} SHARED ${SRC_FILES})
set_target_properties(${PLUGIN_CUSTOM_OUTPUT_FILE} PROPERTIES LINK_FLAGS "-g")
install(TARGETS ${PLUGIN_CUSTOM_OUTPUT_FILE} LIBRARY DESTINATION lib)
